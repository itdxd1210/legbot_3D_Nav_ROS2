# GO2 ROS 2 Jazzy 运行指南

适用环境：Ubuntu 24.04、ROS 2 Jazzy、Gazebo Harmonic、GO2 EDU。

本工程全部功能使用独立 launch 文件启动。推荐按本指南顺序执行，不要同时启动两套
`simulation.launch.py` 或 `real.launch.py`，也不要同时加载两个底层策略。

## 目录

1. [通用准备](#1-通用准备)
2. [Gazebo 组合地形 FAST-LIO + SCAN 完整仿真](#2-gazebo-组合地形-fast-lio--scan-完整仿真)
3. [无界面仿真、组合地图、楼梯和多楼层路线](#3-无界面仿真组合地图楼梯和多楼层路线)
4. [GO2 EDU 真机完整流程](#4-go2-edu-真机完整流程)
5. [航向点操作](#5-航向点操作)
6. [停止、恢复和故障排查](#6-停止恢复和故障排查)

## 1. 通用准备

### 1.1 编译与离线检查

第一次运行或修改代码后执行：

```bash
cd /home/fatu08/legbot_3D_Nav_ROS2
./tools/build.sh
source tools/env.sh
./tools/check_offline.sh
```

离线检查不会启动 Gazebo，也不会连接或控制真机。看到以下结尾才继续：

```text
PASS offline checks; no simulation or robot experiment was started by this check
```

### 1.2 每个新终端的初始化

后续每打开一个新终端，都先执行：

```bash
cd /home/fatu08/legbot_3D_Nav_ROS2
source tools/env.sh
```

### 1.3 策略选择

| 用途 | `policy_profile` | 当前结论 |
| --- | --- | --- |
| 默认平地、楼梯与真机测试 | `moe_cts_77k` | 已通过平地 queue/direct 闭环和 8 级楼梯动力学测试 |
| TorchScript 平地对照 | `go2_cts` | 保留为已验证的对照策略 |
| 崎岖地形与楼梯 | `moe_cts_77k` | 已完成 8 级楼梯底层动力学测试 |
| 平地对照 | `mjlab_flat` | 能持续前进，但当前 Gazebo 模型存在横漂 |
| 旧策略回归 | `robot_lab`、`stairs_trot`、`himloco`、`legged_gym` | 保留用于对照，不用于首次无吊架真机测试 |

仿真和真机入口默认选择 `moe_cts_77k`。

### 1.4 启动前检查残留进程

```bash
ps -eo pid,ppid,pgid,sid,stat,cmd | \
  grep -E 'gz sim|ros2 launch|fastlio|scan_planner|controller_manager|rviz2' | \
  grep -v grep
```

如果没有输出即可启动。若有上一次异常退出留下的进程，只对列表中确认属于本工程的 PID
执行 `kill -INT PID`，复查为空后再继续。

## 2. Gazebo 组合地形 FAST-LIO + SCAN 完整仿真

本节使用 Gazebo 模拟 LiDAR/IMU，经 FAST-LIO 生成定位和注册点云，再由 SCAN 规划路径。
`navigation_source` 默认为 `fastlio`，`publish_ground_truth` 默认为 `false`；因此
Gazebo 真值默认不会进入定位、规划或控制。

下面的命令只写必须覆盖的参数；未列出的参数均使用对应 launch 文件和
`config/scan.yaml` 中的默认值。这样可以避免旧命令显式覆盖后来修正过的默认参数。

### 2.1 终端 1：Gazebo、GO2 和 `moe_cts_77k`

```bash
ros2 launch legbot_bringup simulation.launch.py \
  gui:=true
```

等待终端出现：

```text
Configured and activated rl_quadruped_controller
```

### 2.2 终端 2：站立与闭环状态机

```bash
ros2 launch legbot_bringup go2_demo_control.launch.py
```

机器人会依次进入下蹲、固定站立，并等待 FAST-LIO 与 SCAN 路径。

### 2.3 终端 3：FAST-LIO

```bash
ros2 launch legbot_bringup fastlio.launch.py \
  use_sim_time:=true \
  config:=/home/fatu08/legbot_3D_Nav_ROS2/install/legbot_bringup/share/legbot_bringup/config/fastlio_sim.yaml
```

等待终端出现 `IMU Initial Done`。FAST-LIO 现在只累计连续静止的 IMU 数据：机器人仍在
下蹲、站起或明显晃动时会重新开始初始化，稳定后约 1 秒完成。初始化完成前不要发送导航目标。

### 2.4 终端 4：SCAN

```bash
ros2 launch legbot_bringup scan.launch.py \
  use_sim_time:=true
```

`finish_dist` 是实际的 XY 到达半径。`finish_dist_z` 仅作为显式三维目标、预设航点和
PCT 跨楼层路线的“同层”门限，不参与普通 2D Goal Pose 的到达距离计算。本场景相邻
楼层约相差 4 m，因此默认 1.5 m 可以容忍一定的 FAST-LIO 高度漂移，同时避免机器人
在三楼误认 XY 重合的二楼航点。普通平地 2D 目标保持
`manual_goal_use_message_z:=false`；只有目标消息确实携带楼层高度时才设为 `true`。

SCAN 与原项目一致使用完整点云，不额外分割地面。地面点位于机身
规划高度之下，不会自动变成机身碰撞；地形可通行性由 PCT 负责。以 Robot-Nav 原集成
参数为基线，当前适度增加局部避障距离：双圆柱半径 `0.20 m`、前后偏移 `0.07 m`，
障碍上下膨胀仍为 `0.03 m`，优化器安全距离为 `0.25 m`。相比原值，侧向约增加
`0.10 m`、前向约增加 `0.12 m` 的规划余量；仍明显小于此前容易绕大圈的参数。
命令适配器不再订阅点云做第二次急停或制动，
避障统一由 SCAN 的占据图、膨胀、轨迹优化和碰撞检查负责。

局部规划默认前视 `3.0 m`，计划位置离本段起点 `0.2 m` 后即可进行下一次常规滚动重规划；
按 `0.5 m/s` 上限约为 `0.4 s`。运行时安全检查、初始轨迹分段检查和优化后复检都会检查
完整局部轨迹，不再忽略末端 `1/3`。

SCAN 默认只使用雷达 X 正方向前方 `240°`（左右各 `120°`）的点云。其余后方回波不会参与
射线更新、占据标记、膨胀或避障，转向后落到当前后半球的历史占据也会清除；FAST-LIO
仍使用完整 `360°` 点云定位。该模式不为倒退提供后方防撞保护，应以向前行走为主。
需要恢复全向避障时启动 SCAN 时设置
`lidar_horizontal_fov_deg:=360 lidar_clear_outside_fov:=false`。

默认雷达位姿由 FAST-LIO 雷达 IMU 位姿转换到
`livox_link`，注册点云和雷达位姿按同一扫描结束时间戳配对，避免用下一帧位姿建上一帧地图。
与原项目一致，SCAN 不解析 URDF link 做自身点云过滤；`robot_description` 只用于
`robot_state_publisher` 和 RViz 模型/TF。规划碰撞检查仍使用前后双圆柱机身模型；雷达近场
盲区由 FAST-LIO 的 `blind` 参数处理，SCAN 不重复过滤。RViz 的 `Active planner occupancy` 是机身高度带内的
**膨胀占据栅格**，不是原始点云。黄色 `Manual XY goal (estimated floor)` 标记点击的 XY
在机身高度减站立高度所得的估计地面上的投影，不是实测地面；青色
`SCAN body-height planning goal` 是机身规划高度；到达仍按 XY
判定。普通 2D 模式的局部参考只按 XY 投影，其机身规划高度会平滑跟随当前里程计，
不再追逐点击时冻结的旧 Z；显式 3D/跨楼层目标仍保留其高度变化。

如果需要启动 PCT，先在已 `source install/setup.bash` 的终端检查本地动态库环境：

```bash
ros2 run pct_planner pct_plan --check-libraries
```

检查会验证 Python 3.12、`libmetis-gtsam.so` 以及 PCT 原生模块是否可加载。单独执行
`pct_plan.launch.py` 只发布 Building 局部坐标中的原始 PCT 路径，不会驱动机器人；
完整自动执行应使用第 3.2 节的 `pct_cross_floor_demo.launch.py`。默认库目录是
`third_party/pct/install/lib`，仅在安装位置不同的机器上设置
`PCT_THIRDPARTY_LIB_DIR`。检查不会启动 PCT、Gazebo 或 RViz。
默认使用工作区 `.venv-jazzy/bin/python3`；若 Python 3.12 环境另装在别处，可设置
`PCT_PYTHON` 为其可执行文件路径。

### 2.5 终端 5：RViz2

```bash
ros2 launch legbot_bringup scan_rviz.launch.py
```

### 2.6 终端 6：RViz 航向点

默认使用 `direct`。每次新的 **2D Goal Pose** 会替换当前目标，但仍由 SCAN 规划和避障。
首次进入 `finish_dist=0.40 m` 的 XY 到达圈后，命令适配器锁存零速度，并带当前
目标时间戳发布 `/scan/goal_stop_ack`；航点管理节点确认这是当前目标且到达半径匹配，
短暂停留后发送任务完成并让 GO2 切换固定站立。普通 2D 目标不按 Z 判定。
如需修改到达半径，请让终端 4 的 `finish_dist` 与终端 6 的 `reach_xy` 保持一致；
前者决定何时锁存零命令，后者核验停止确认，否则可能停下却不宣布任务完成。

```bash
ros2 launch legbot_bringup scan_waypoints.launch.py
```

### 2.7 打点与验收

等待终端 2 输出：

```text
FAST-LIO ready: enabling SCAN waypoint mission
```

然后在 RViz2 顶部选择 **2D Goal Pose**：

1. 先在机器人前方约 2 m 的空旷位置打点。
2. 确认 `SCAN local B-spline path` 出现绿色局部路径。
3. 确认 GO2 朝路径方向行走并在目标附近停止。
4. 再把目标放到障碍物后方，确认 B 样条发生横向绕行。
5. 确认 `/fast_lio/odometry_base` 持续更新且没有使用 `/Odometry_gazebo`。

唯一保留的组合世界范围为 x=[-32,32] m、y=[-16,16] m。GO2 从 (-27,6) m 出生，西半区
30 个圆柱、3 道分段弧顶拱门和场地边界为 FAST-LIO 提供几何约束；东半区为深灰蓝色
Building 多楼层楼梯。SCAN 默认规划速度上限为 0.50 m/s，代码硬上限为 0.75 m/s。

如需暂时用 Gazebo 真值排除 FAST-LIO 影响，终端 1 显式使用
`publish_ground_truth:=true navigation_source:=ground_truth`，不启动终端 3，并将终端
2、4、6 分别改为：

```bash
ros2 launch legbot_bringup go2_demo_control.launch.py \
  odom_topic:=/Odometry_gazebo cloud_topic:=/livox/points_world \
  localization_label:="Gazebo ground truth"

ros2 launch legbot_bringup scan.launch.py \
  odom_topic:=/Odometry_gazebo cloud_topic:=/livox/points_world \
  sensor_pose_topic:=/go2/lidar_pose

ros2 launch legbot_bringup scan_waypoints.launch.py \
  odom_topic:=/Odometry_gazebo
```

三个节点必须使用同一套真值输入。若终端 2 仍用默认 FAST-LIO 话题，
`/go2/demo_ready` 不会就绪，此时 RViz 的 2D Goal 会被航点节点主动拒绝。

要在导航仍使用 FAST-LIO 时量化它相对 Gazebo 真值的漂移，终端 1 可使用：

```bash
ros2 launch legbot_bringup simulation.launch.py \
  gui:=true publish_ground_truth:=true diagnose_fastlio:=true
```

`navigation_source` 仍为默认的 `fastlio`，所以真值只被
`fastlio_truth_monitor` 读取，不进入控制和规划。监视器先对齐两套里程计的初始
XY/Z/航向，然后每 2 s 输出当前误差、RMSE 和最大误差。这种比较模式不桥接
Gazebo 的 `odom -> base` TF，避免与 FAST-LIO 的 TF 发布者冲突。

### 2.8 仿真停止顺序

1. 清除航向点。
2. 依次停止终端 6、5、4、3、2。
3. 最后停止终端 1，并等待 Gazebo 完全退出。

```bash
ros2 service call /scan/waypoints/clear std_srvs/srv/Trigger '{}'
```

## 3. 无界面仿真、组合地图、楼梯和多楼层路线

### 3.1 无界面组合地形测试

仍按第 2 节的独立 launch 顺序启动，只做两处修改：

1. 终端 1 把 `gui:=true` 改为 `gui:=false`。
2. 不启动 RViz 和 `scan_waypoints.launch.py`，直接向 SCAN 发布目标。

例如先发送出生点前方的短距离目标：

```bash
ros2 topic pub --once /scan/goal geometry_msgs/msg/PoseStamped \
  '{header: {stamp: now, frame_id: odom}, pose: {position: {x: -23.0, y: 6.0, z: 0.0}, orientation: {w: 1.0}}}'
```

检查结果：

```bash
ros2 topic hz /fast_lio/odometry_base
ros2 topic hz /fast_lio/cloud_registered
ros2 topic hz /scan/planning/bspline
ros2 topic echo /scan/mission_complete
```

长距离无界面实验会持续占用 Gazebo、FAST-LIO 和点云处理资源。一次只运行一套仿真，完成
后按第 2.8 节顺序关闭，避免多个 Gazebo 进程叠加导致卡机。

### 3.2 平地障碍区接入 PCT 跨层全局路径的自动全程测试

`Building.sdf` 是默认组合测试地图，不需要额外指定 `world`、`x` 或 `y`。场景划分如下：

| 区域 | 范围或位置 | 内容 |
| --- | --- | --- |
| 整体场地 | 64 m × 32 m | 四周有 1.2 m 高边界墙，便于识别边界和 LiDAR 定位 |
| 西半区 | x=[-32,0] m | 水平地面、30 个错列圆柱和 3 道可穿越拱门，全部带碰撞 |
| 拱门中心 | (-21,6)、(-11,6)、(-1,6) m | 约 2.1 m 净宽，弧顶由 5 段碰撞体组成 |
| 分界线 | x=0 m | 蓝色视觉标线，无碰撞，不会产生台阶 |
| 东半区 | x=[0,32] m | 深灰蓝色 Building 多楼层楼梯，模型原点位于 (13,0)，西侧边缘距平地区约 3.6 m |
| 默认出生点 | (-27,6,0.50) m | 朝向 x 正方向，先通过平地障碍区，再接近 Building |
| Building 入口前 | 约 (7.5,6) m | 保留原模型朝向后的主要入口接近位置 |

完整任务使用一个 launch 自动启动 Gazebo、GO2、PCT、SCAN、任务状态机和 RViz。
运行前不要另外启动第 2 节中的六个进程：

```bash
cd /home/fatu08/legbot_3D_Nav_ROS2
source tools/env.sh

ros2 launch legbot_bringup pct_cross_floor_demo.launch.py \
  gui:=true rviz:=true
```

这条长距离跨层任务默认使用 `navigation_source:=ground_truth`。原因是仿真 FAST-LIO 在
长平地接驳段已经观测到明显的累计高度漂移；例如一次入口失败时，机器人仍在地面，
`/fast_lio/odometry_base.z` 却已从约 0.2 m 漂到 -1.99 m，使楼梯首段被规划器误认为
2 m 以上的垂直跳跃。真值模式只替换仿真定位与点云坐标基准，PCT 的 105 点跨层路线、
SCAN 的局部 B 样条避障和 GO2 底层策略全部照常运行。

如果要专门复现或调试 FAST-LIO 长距离定位链，可显式运行：

```bash
ros2 launch legbot_bringup pct_cross_floor_demo.launch.py \
  navigation_source:=fastlio
```

该模式目前不建议用来验收完整跨层到达。默认命令还会显式加载
`scan_stairs_demo.rviz`，避免其他 include 中同名 `config` 参数覆盖 RViz 配置。
一键 launch 同时显式固定 SCAN 的可视化坐标系为 `odom`，并让 RViz 相机跟随 GO2 的
`base`；因此占据点云、局部路径、动态目标和机器人模型应随机器人一起移动显示。

启动后不需要在 RViz 手动打点。系统执行顺序为：

1. GO2 在 (-27,6) m 出生，完成下蹲和固定站立；
2. Gazebo 真值里程计和世界坐标点云就绪，PCT 从预生成 `building2_9` tomogram 计算跨层路径；
3. 任务桥用当前 GO2 位姿自动标定 PCT Building 坐标系到 `odom` 的变换；
4. SCAN 在路径开头插入 GO2 的当前位置，形成“当前位置到 Building 入口”的平地接驳段；
5. GO2 沿接驳段穿过圆柱和拱门区，再跟随 PCT 路径走完多层楼梯；
6. 终点到达确认后锁存零速度，并自动切回固定站立。

该任务单独使用 0.50 m 的终点确认半径。实测 GO2 在顶层最终点减速后会停在约 0.44 m
误差处；继续沿用普通点目标的 0.40 m 阈值会形成“已停稳但差 4 cm 不确认”的末端死区。
此调整只作用于一键 PCT 跨层 launch，不改变第 2 节普通手动导航的 0.40 m 配置。

任务开始时应依次看到：

```text
Gazebo ground truth ready: enabling SCAN waypoint mission
PCT cross-floor mission started: connector=...
SCAN trajectory accepted: enabling configured RL walking policy
```

任务完成时应看到：

```text
PCT cross-floor mission complete; GO2 may return to fixed stand
```

PCT 原始路线使用 Building 局部坐标，入口约为 (-5.5,6,0.56)，跨层终点约为
(1.95,-2.99,5.03)。Building 在 Gazebo 世界中的原点为 (13,0,0)，但导航使用的
`odom` 原点取决于所选定位源，因此任务桥不会只使用固定的“加 13 m”；它根据
Gazebo 出生位姿和任务启动时的当前位姿计算完整二维刚体变换并保留相对楼层高度。
默认情况下，当前位置到 PCT 入口的接驳距离约为 34.5 m。

仓库中的原始 PCD 是 Git LFS 指针；自动任务直接加载已经生成且随包安装的
`building2_9.pickle` tomogram，因此运行这条路线不需要重新下载 PCD 或重新执行层析图
生成。PCT 规划节点会从该 tomogram 重建 RViz 用的 `/global_points` 和
`/tomogram` 点云，两个话题均使用 transient-local QoS；即使 RViz 晚于规划节点
启动也能收到地图。这些点云的 `frame_id` 是 `building_pct`。默认的 Gazebo
真值模式会在启动时立即发布 `odom -> building_pct` 静态 TF，不需要等待机器人开始行走；
FAST-LIO 模式则由任务桥在起点标定后发布该 TF，使地图与 GO2 和 PCT 路径正确对齐。当前使用稳定的
PCT 多层 A* 全局路径；SCAN 再负责局部 B 样条、动态约束和实时避障。

到达 Building 入口前应分别确认：

1. 西半区地面始终水平，机器人没有与圆柱或边界墙接触；
2. `SCAN local B-spline path` 在圆柱前发生合理的侧向绕行；
3. `/fast_lio/odometry_base` 和 `/fast_lio/cloud_registered` 持续更新；
4. 越过 x=0 的蓝线时没有高度跳变或碰撞；
5. 进入 Building 前机器人能够重新对准入口，而不是贴着外墙前进。

本任务中 SCAN 使用 `navi_mode:=3` 订阅 `/pct_path`，并启用终点 Z 的同层判定。
不要同时启动 `scan_waypoints.launch.py` 或再发送 RViz 2D Goal，否则会引入第二套任务
管理入口。

如果只想观察地图和手动控制，不运行 FAST-LIO/SCAN，可关闭导航数据要求：

```bash
ros2 launch legbot_bringup simulation.launch.py \
  gui:=true enable_lidar:=false require_navigation_data:=false
```

该命令只启动 Gazebo、GO2 和底层策略，不会自动让机器人穿越障碍或爬楼。完整自动闭环
应使用本节的 `pct_cross_floor_demo.launch.py`。

### 3.3 楼梯底层策略演示

`moe_cts_77k` 已在 8 级楼梯场景完成底层动力学测试。现在只保留组合世界；如需从
Building 入口附近单独观察底层策略，使用：

```bash
ros2 launch legbot_bringup simulation.launch.py \
  gui:=true enable_lidar:=false \
  require_navigation_data:=false policy_profile:=moe_cts_77k \
  x:=7.5 y:=6.0 z:=0.50
```

这个命令仍加载完整组合世界，但把机器人直接放在 Building 入口附近；它不会自动发送
前进命令。底层直行爬楼结果不能代替完整的
FAST-LIO + SCAN 楼梯闭环结论。

### 3.4 多楼层关键点录制

平地临时目标使用 direct/queue。楼梯和跨楼层重复路线使用 `keypoint_recorder`。仿真时
另开终端：

```bash
ros2 launch legbot_bringup scan_keypoint_recorder.launch.py output:=/home/fatu08/legbot_3D_Nav_ROS2/data/routes/scan_keypoints.yaml
```

每到一个需要保存的位置执行：

```bash
ros2 service call /scan/keypoints/record_current std_srvs/srv/Trigger '{}'
```

撤销、清空、保存和显示整条路线：

```bash
ros2 service call /scan/keypoints/undo std_srvs/srv/Trigger '{}'
ros2 service call /scan/keypoints/clear std_srvs/srv/Trigger '{}'
ros2 service call /scan/keypoints/save std_srvs/srv/Trigger '{}'
ros2 service call /scan/keypoints/publish_route std_srvs/srv/Trigger '{}'
```

回放时不要启动 `scan_waypoints.launch.py`，把 SCAN 改为：

```bash
ros2 launch legbot_bringup scan.launch.py \
  use_sim_time:=true navi_mode:=2 \
  keypoints_yaml:=/home/fatu08/legbot_3D_Nav_ROS2/data/routes/scan_keypoints.yaml \
  manual_goal_use_message_z:=true
```

录制和回放必须使用相同的 FAST-LIO 地图坐标原点。

## 4. GO2 EDU 真机完整流程

真机按以下阶段逐项验收：

1. 网络与只读传感器预检。
2. 单独测试 `moe_cts_77k`，暂时不启动 FAST-LIO 和 SCAN。
3. 关闭电机输出，验证原装 L1 和 FAST-LIO。
4. 保持电机输出关闭，验证 SCAN 能生成避障路径。
5. 重新放行 `moe_cts_77k`，完成 FAST-LIO + SCAN + GO2 闭环。

### 4.1 网络拓扑

GO2 的 `LowState`、`LowCmd`、原装 L1 点云和 L1 内部 IMU 都通过同一根网线上的 DDS
传输。一台电脑的一个以太网口即可运行控制、FAST-LIO、SCAN 和 RViz，不需要把工程复制
到 GO2 内置电脑。

本机网卡名为 `enp131s0`，GO2 默认地址按 `192.168.123.161` 配置：

```bash
sudo ip link set enp131s0 up
sudo ip address replace 192.168.123.99/24 dev enp131s0
ip -4 -brief address show dev enp131s0
ping -I enp131s0 -c 3 192.168.123.161
```

### 4.2 阶段一：只读预检

```bash
cd /home/fatu08/legbot_3D_Nav_ROS2
source tools/env.sh
./tools/go2_real_preflight.sh enp131s0
```

该工具只读取 12 个关节、机身 IMU、L1 内部 IMU 和点云字段，不发布 `LowCmd`。看到：

```text
GO2_READONLY_PREFLIGHT=PASS ... lidar_fields=PASS
```

预检结束后会自动关闭临时进程。若失败，把终端输出和
`data/real_preflight/时间戳/` 内的日志发回来。

### 4.3 阶段二：单独测试 `moe_cts_77k`

这一阶段不启动 FAST-LIO 和 SCAN。机器人应位于平整、防滑的空地，首次站立和行走建议
使用吊架，遥控器保持在手边。

在独立终端先启动键盘急停：

```bash
ros2 run legbot_bringup go2_estop_keyboard
```

保持该终端在前台。键位为：`1` 进入 Passive 阻尼，`2` 执行下一次固定下蹲/站立
转换，`3` 进入 RL，`W/S` 以 0.02 m/s 递增/递减前向速度，`X` 立即归零。默认键盘速度
限制为 `[-0.10, 0.10] m/s`，且在按 `3` 请求 RL 之前会拒绝 `W/S`。

按空格或 `Esc` 会锁存急停，直接使 RL 控制器进入
`Kp=0, Kd=1, dq=0, tau=0` 的被动阻尼状态。键盘急停终端中的 `Ctrl+C` 也会先触发急停
再退出。急停一旦触发不接受运动指令，检查现场安全后必须重启 `real.launch.py`
才能解除。阻尼会让机器人失去支撑，因此仍必须使用吊架。

#### 4.3.1 释放官方高层运动模式

```bash
ros2 launch legbot_bringup go2_mode.launch.py \
  network_interface:=enp131s0 action:=release \
  confirm_action:=true
```

#### 4.3.2 终端 A：启动真机硬件和 `moe_cts_77k`

单策略测试没有 FAST-LIO，所以必须设置 `require_navigation_data:=false`。这里的速度参数
只是安全上限，不会使机器人自动行走。
真机 controller_manager 默认使用 400 Hz 硬件循环，与 200 Hz 策略控制及 50 Hz
推理保持整数倍频率，并避免非实时 Linux 主机在 1000 Hz 下持续超期。

```bash
ros2 launch legbot_bringup real.launch.py \
  network_interface:=enp131s0 enable_commands:=true \
  require_navigation_data:=false
```

等待出现控制器成功配置和激活的提示。此时没有速度和模式输入，控制器保持无运动请求。

#### 4.3.3 终端 B：下蹲、站立并进入 RL

第一次 `mode=2` 从被动状态进入固定下蹲：

```bash
ros2 topic pub -r 20 -t 20 /go2/control_mode std_msgs/msg/Int32 '{data: 2}'
sleep 4
```

第二次 `mode=2` 从固定下蹲进入固定站立：

```bash
ros2 topic pub -r 20 -t 20 /go2/control_mode std_msgs/msg/Int32 '{data: 2}'
sleep 4
```

确认四条腿受力正常、机身高度稳定后，用 `mode=3` 进入 `moe_cts_77k` RL 状态：

```bash
ros2 topic pub -r 20 -t 20 /go2/control_mode std_msgs/msg/Int32 '{data: 3}'
```

先发送 2 秒零速度，验证原地站立：

```bash
ros2 topic pub -r 20 -t 40 /cmd_vel geometry_msgs/msg/Twist \
  '{linear: {x: 0.0, y: 0.0}, angular: {z: 0.0}}'
```

#### 4.3.4 低速短距离行走

先以 0.10 m/s 前进约 2 秒：

```bash
ros2 topic pub -r 20 -t 40 /cmd_vel geometry_msgs/msg/Twist \
  '{linear: {x: 0.10, y: 0.0}, angular: {z: 0.0}}'
```

立即发送零速度并观察 2 秒：

```bash
ros2 topic pub -r 20 -t 40 /cmd_vel geometry_msgs/msg/Twist \
  '{linear: {x: 0.0, y: 0.0}, angular: {z: 0.0}}'
```

只有当步态、机身高度和停止动作都正常时，再测试 0.20 m/s、约 2 秒：

```bash
ros2 topic pub -r 20 -t 40 /cmd_vel geometry_msgs/msg/Twist \
  '{linear: {x: 0.20, y: 0.0}, angular: {z: 0.0}}'

ros2 topic pub -r 20 -t 40 /cmd_vel geometry_msgs/msg/Twist \
  '{linear: {x: 0.0, y: 0.0}, angular: {z: 0.0}}'
```

#### 4.3.5 结束单策略测试

从 RL 回固定站立，再回固定下蹲，最后进入被动状态：

```bash
ros2 topic pub -r 20 -t 20 /go2/control_mode std_msgs/msg/Int32 '{data: 2}'
sleep 4
ros2 topic pub -r 20 -t 20 /go2/control_mode std_msgs/msg/Int32 '{data: 2}'
sleep 4
ros2 topic pub -r 20 -t 20 /go2/control_mode std_msgs/msg/Int32 '{data: 1}'
```

停止终端 A 的 `real.launch.py`，然后恢复官方 `normal` 模式：

```bash
ros2 launch legbot_bringup go2_mode.launch.py \
  network_interface:=enp131s0 action:=select \
  confirm_action:=true
```

只有这一阶段的站立、零速、前进和停车全部正常，才继续 FAST-LIO 与 SCAN。

### 4.4 阶段三：原装 L1 和 FAST-LIO，只读运行

以下三个终端都不发送电机命令。

#### 4.4.1 终端 1：原装 L1 DDS 桥

```bash
ros2 launch legbot_bringup unitree_lidar.launch.py network_interface:=enp131s0
```

它发布：

- `/unitree/lidar`：原装 L1 点云。
- `/unitree/lidar_imu`：L1 内部 IMU。

#### 4.4.2 终端 2：GO2 状态和 TF

```bash
ros2 launch legbot_bringup real.launch.py network_interface:=enp131s0
```

#### 4.4.3 终端 3：FAST-LIO

默认使用 L1 点云和 L1 内部 IMU：

```bash
ros2 launch legbot_bringup fastlio.launch.py \
  sensor_frame:=utlidar_imu \
  config:=/home/fatu08/legbot_3D_Nav_ROS2/install/legbot_bringup/share/legbot_bringup/config/fastlio_unitree_l1.yaml
```

机器人静置至少 10 秒完成 IMU 初始化，再分别检查：

```bash
ros2 topic hz /unitree/lidar
ros2 topic hz /unitree/lidar_imu
ros2 topic hz /fast_lio/odometry_base
ros2 topic hz /fast_lio/cloud_registered
```

L1 默认参数为 18 线、逐点 `time` 单位为秒；雷达在 L1 IMU 坐标系中的平移为
`[0.007698, 0.014655, -0.00667] m`，旋转为单位阵。

如果只读预检明确报告 L1 内部 IMU 异常，终端 3 改用机身 IMU 备用配置：

```bash
ros2 launch legbot_bringup fastlio.launch.py \
  sensor_frame:=imu_link \
  config:=/home/fatu08/legbot_3D_Nav_ROS2/install/legbot_bringup/share/legbot_bringup/config/fastlio_unitree_l1_body_imu.yaml
```

### 4.5 阶段四：SCAN 只规划，不驱动 GO2

保持第 4.4 节三个终端运行，并继续保持终端 2 的 `enable_commands:=false`。

#### 4.5.1 终端 4：SCAN 低速真机参数

```bash
ros2 launch legbot_bringup scan.launch.py \
  max_vel:=0.20 max_vy:=0.08 max_vyaw:=0.30 max_acc:=0.20
```

#### 4.5.2 终端 5：RViz2

```bash
ros2 launch legbot_bringup scan_rviz.launch.py use_sim_time:=false
```

#### 4.5.3 终端 6：direct 航向点

```bash
ros2 launch legbot_bringup scan_waypoints.launch.py use_sim_time:=false
```

在 RViz 中打一个空旷区域目标，确认以下内容：

1. 原装雷达点云与 GO2 模型方向一致。
2. 地面没有整体进入膨胀障碍层。
3. `/scan/planning/bspline` 持续发布有效局部路径。
4. 障碍物位于直线路径上时，B 样条发生绕行。
5. 因 `enable_commands:=false`，GO2 不会行走。

### 4.6 阶段五：FAST-LIO + SCAN + `moe_cts_77k` 真机闭环

先清除目标并停止第 4.4.2 节的只读 `real.launch.py`，其余 L1、FAST-LIO、SCAN、RViz 和
航向点终端保持运行：

```bash
ros2 service call /scan/waypoints/clear std_srvs/srv/Trigger '{}'
```

再次释放官方运动模式：

```bash
ros2 launch legbot_bringup go2_mode.launch.py \
  network_interface:=enp131s0 action:=release \
  confirm_action:=true
```

#### 4.6.1 终端 2：带导航安全门启动 `moe_cts_77k`

完整闭环必须保持 `require_navigation_data=true`（默认值）。FAST-LIO 里程计或注册点云
超时后，速度入口会自动归零。

```bash
ros2 launch legbot_bringup real.launch.py \
  network_interface:=enp131s0 enable_commands:=true
```

#### 4.6.2 终端 7：站立与 SCAN 联动状态机

```bash
ros2 launch legbot_bringup go2_demo_control.launch.py use_sim_time:=false
```

等待终端 7 输出：

```text
FAST-LIO ready: enabling SCAN waypoint mission
```

然后在 RViz 中先发送约 0.5 m 正前方的空旷目标。SCAN 生成有效 B 样条后，状态机才进入
RL 行走。按以下顺序验收：

1. 0.5 m 空旷直行与停车。
2. 约 1 m 空旷直行与停车。
3. 低速靠近但不穿过障碍物，确认安全门停车。
4. 将目标放到障碍物侧后方，确认 SCAN 重规划并绕行。

首次真机保持 0.20 m/s。完成以上测试后，再把 SCAN 的 `max_vel` 和 `real.launch.py` 的
`max_vx` 同时提高到 0.30 m/s；每次只增加一个速度等级，不直接使用仿真的 0.50 m/s。

### 4.7 真机完整停止顺序

1. 清除航向点。
2. 停止终端 6 航向点节点。
3. 停止终端 4 SCAN。
4. 停止终端 7 联动状态机。
5. 确认 GO2 已回到固定站立或固定下蹲。
6. 停止终端 2 `real.launch.py`。
7. 停止终端 3 FAST-LIO、终端 1 L1 桥和终端 5 RViz。
8. 恢复官方 `normal` 模式。

```bash
ros2 service call /scan/waypoints/clear std_srvs/srv/Trigger '{}'

ros2 launch legbot_bringup go2_mode.launch.py \
  network_interface:=enp131s0 action:=select \
  confirm_action:=true
```

## 5. 航向点操作

### 5.1 direct 模式

每个新目标立即替换当前目标，适合平地手动演示。direct 目标仍进入 SCAN，不会直接写入
`cmd_vel`。

```bash
ros2 service call /scan/waypoints/set_direct_mode std_srvs/srv/SetBool '{data: true}'
```

### 5.2 queue 模式

多个点按顺序执行：黄色为当前点，蓝色为等待点，绿色为已完成点。

```bash
ros2 service call /scan/waypoints/set_direct_mode std_srvs/srv/SetBool '{data: false}'
```

在 RViz 顶部选择 **Publish Point**，点击蓝色或黄色目标球体 0.5 m 范围内可删除该点。
也可以使用：

```bash
ros2 service call /scan/waypoints/undo std_srvs/srv/Trigger '{}'
ros2 service call /scan/waypoints/clear std_srvs/srv/Trigger '{}'
ros2 topic echo /scan/waypoint_mode --once
```

### 5.3 真机多楼层关键点

真机录制时将第 3.4 节的 `use_sim_time:=true` 改为 `false`。操作者先遥控 GO2 到楼梯
入口、平台、转角和出口，再依次记录当前 FAST-LIO XYZ。回放时使用 `navi_mode:=2`，并
保持录制与回放的 FAST-LIO 地图原点一致。

## 6. 停止、恢复和故障排查

### 6.1 默认策略能站立但不前进

```bash
ros2 topic hz /control_input
ros2 topic echo /cmd_vel
ros2 control list_controllers
```

单策略测试必须使用 `require_navigation_data:=false`；完整 SCAN 闭环必须使用
`require_navigation_data:=true`，并保证 FAST-LIO 里程计和注册点云持续更新。

### 6.2 FAST-LIO 没有输出

```bash
ros2 topic hz /unitree/lidar
ros2 topic hz /unitree/lidar_imu
ros2 topic info -v /unitree/lidar_imu
ros2 topic echo /unitree/lidar --once --field fields
```

点云必须包含 `x`、`y`、`z`、`intensity`、`time`、`ring`。启动 FAST-LIO 前让 GO2 静置
至少 10 秒。

### 6.3 RViz 显示 `Empty topic name`

确认使用本工程安装后的 RViz launch，并在重新编译后重新 source：

```bash
cd /home/fatu08/legbot_3D_Nav_ROS2
source tools/env.sh
ros2 launch legbot_bringup scan_rviz.launch.py use_sim_time:=false
```

### 6.4 Gazebo 或电脑卡顿

通常是旧 Gazebo、RViz、FAST-LIO 或规划器没有退出。使用第 1.4 节命令检查；不要直接
重复启动新的六终端流程。

### 6.5 外接 Mid-360

只有改用外接 Mid-360 时才启动 `livox_ros_driver2` 并使用 `fastlio_real.yaml`。现场必须
修改 `MID360_config.json` 的主机 IP、雷达 IP 和实际外参。原装 L1 方案不启动 Livox
驱动。

上游接口参考：

- Unitree SDK2：https://github.com/unitreerobotics/unitree_sdk2
- Unitree ROS2：https://github.com/unitreerobotics/unitree_ros2
- Unitree L1 SDK：https://github.com/unitreerobotics/unilidar_sdk
- Unitree L1 Point-LIO 参数：https://github.com/unitreerobotics/point_lio_unilidar
- Livox ROS Driver 2：https://github.com/Livox-SDK/livox_ros_driver2
